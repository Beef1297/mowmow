#include<stdio.h>
#include<stdlib.h>
#include<string.h>
char buf[128];


int main(void){
  char str[128];
  fgets(buf, sizeof(buf), stdin);
  strcpy(str, buf);
  printf("Hello World!\n");
  puts("poo poo");
  return 0;
}
